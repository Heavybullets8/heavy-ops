apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-speed-pvc
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: iscsi-speed

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-tank-pvc
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Gi
  storageClassName: iscsi-tank
