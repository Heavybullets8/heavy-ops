# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app immich
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template

  values:
    controllers:
      app:
        annotations:
          reloader.stakater.com/auto: "true"

        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18
          update-extensions:
            dependsOn: init-db
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18
            command: ["/bin/bash", "-c"]
            args:
              - |
                export PGHOST="$INIT_POSTGRES_HOST"
                export PGUSER="$${INIT_POSTGRES_SUPER_USER:-postgres}"
                export PGPASSWORD="$INIT_POSTGRES_SUPER_PASS"
                until pg_isready -q; do sleep 1; done
                psql -d "$INIT_POSTGRES_DBNAME" -c "ALTER EXTENSION vchord UPDATE;" || true

        containers:
          immich-server:
            nameOverride: immich-server
            image:
              repository: ghcr.io/immich-app/immich-server
              tag: v3.2.0
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec: &spec
                  httpGet:
                    path: /api/server/ping
                    port: &server-port 2283
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
              startup:
                <<: *probes
                spec:
                  <<: *spec
                  failureThreshold: 30
                  periodSeconds: 5
                  successThreshold: 1
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 5Gi

          immich-ml:
            nameOverride: immich-ml
            dependsOn: immich-server
            image:
              repository: ghcr.io/immich-app/immich-machine-learning
              tag: v3.2.0-cuda
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec: &spec
                  httpGet:
                    path: /ping
                    port: 3003
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
              startup:
                <<: *probes
                spec:
                  <<: *spec
                  failureThreshold: 30
                  periodSeconds: 5
                  successThreshold: 1
            resources:
              requests:
                cpu: 100m
                memory: 3Gi
              limits:
                memory: 20Gi
                nvidia.com/gpu: 1

        defaultContainerOptions:
          env:
            # General
            TZ: ${TIMEZONE}
            IMMICH_LOG_LEVEL: verbose
            IMMICH_TRUSTED_PROXIES: 10.96.0.0/16
            IMMICH_MACHINE_LEARNING_URL: http://127.0.0.1:3003
            # Redis
            REDIS_HOSTNAME: dragonfly.database.svc.cluster.local
            REDIS_DBINDEX: "2"
            # Database
            DB_VECTOR_EXTENSION: vectorchord
          envFrom:
            - secretRef:
                name: immich-secret

    service:
      app:
        controller: app
        ports:
          http:
            port: *server-port

    route:
      app:
        annotations:
          external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
        hostnames:
          - "photos.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-external
            namespace: network

    persistence:
      library:
        existingClaim: *app
        globalMounts:
          - path: /usr/src/app/upload

      # Where the machine learning models are stored
      cache:
        existingClaim: immich-cache
        globalMounts:
          - path: /cache
